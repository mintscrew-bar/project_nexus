# Font Files Placeholder

This directory is intended to hold custom font files (e.g., BeaufortforLOL-Regular.woff2, Spiegel-Regular.woff2) for the Next.js application.

The `layout.tsx` file references these files. Please place the actual font files here, or update the paths in `layout.tsx` if they are located elsewhere.