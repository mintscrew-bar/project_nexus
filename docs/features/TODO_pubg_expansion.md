# 배틀그라운드 확장 기획

> 작성일: 2026-09-03
> 목적: 롤 전용으로 만들어진 Nexus에 두 번째 게임(PUBG)을 얹는다.
> 전제: 롤 파이프라인을 깨지 않는다. 배그를 붙이려다 지금 도는 것을 멈추면 실패다.

---

## 왜 이 기획인가 — 진단

### 1. 데이터 수집은 배그가 오히려 쉽다

롤에서 내전 자동 집계를 막고 있던 건 "코드 없는 사설방은 match-v5에 안 잡힌다"였다.
토너먼트 코드가 유일한 경로였고, 그마저 프로덕션 키 승인이 지연돼 stub 모드다.

배그는 이 벽이 없다.

| | 롤 | 배그 |
|---|---|---|
| 커스텀 매치 조회 | 토너먼트 코드 발급분만 | **일반 전적에 그대로 잡힘** (`isCustomMatch: true`) |
| 승인 절차 | 프로덕션 키 승인 필요 | 무료 키로 즉시 |
| 보존 | 캐시하면 영구 | **최근 2주** |
| 레이트 | 앱 전체 100req/2분 | 기본 **10req/분** (상향 신청 가능) |
| 매치 생성 | API 불가 | API 불가 (동일) |

**10req/분으로 충분하다.** 배틀로얄은 매치 1건에 전 팀·전원의 순위와 킬이 다 들어 있어,
20팀 80명짜리 라운드도 `플레이어 전적 조회 1 + 매치 상세 1` = **2콜**이면 끝난다.
롤처럼 인원수만큼 긁을 일이 없다. 라운드 사이 간격(보통 10분 이상)을 감안하면 예산은 남는다.

### 2. 진짜 벽은 경기 포맷이다

| | 롤 | 배그 |
|---|---|---|
| 구조 | 2팀 5v5, 팀 vs 팀 | 16~25팀이 한 매치에 동시 참가 |
| 승부 | 단판/다전제 승패 | 순위 점수 + 킬 점수 합산 |
| 진행 | 대진표(토너먼트) | 여러 라운드 누적 → 포인트 리더보드 |
| 포지션 | 5라인 고정 | 없음 |

`Match`/`MatchSeries`가 `teamAId`/`teamBId` 2팀 고정이라 배틀로얄을 표현할 수 없다.
`bracket`·`role-selection` 모듈은 배그에 개념 자체가 없다.

### 3. 코드에 게임 축이 없다 (실측 2026-09-03)

```
스키마 65개 모델 중 10개가 롤 전용
  RiotAccount, RiotAccountRoleTier, ChampionSeasonStat, ChampionScanState,
  ChampionMastery, ChampionPreference, SummonerSeasonTier, KnownPuuid,
  RiotMatchCache, MatchRosterSnapshot

API  235파일 중 riot 49 / tier 27 / puuid 20 / 라인 19   (~20%)
웹   300파일 중 tier 47 / champion 40 / riot 38 / 라인 34 (~15%)

Room 에 게임 구분 필드 없음
teamCountForRoomSize(n) = Math.floor(n / 5)   ← 하드코딩 (packages/types/src/series-preset.ts:225)
enum Role { TOP JUNGLE MID ADC SUPPORT }      ← 밸런스·경매·역할선택·대진표가 전부 의존
```

---

## 설계 원칙

1. **롤을 멈추지 않는다.** 게임 축을 넣는 리팩터링은 기능 변화 0으로 끝나야 한다.
2. **대진표를 배그에 우겨넣지 않는다.** 배그는 포인트 리더보드다. 새 모델을 판다.
3. **재사용은 "사람" 쪽에서만.** 계정·클랜·커뮤니티·디스코드·방 로비는 그대로. 게임 진행은 별개.
4. **포인트 규칙은 하드코딩하지 않는다.** 내전마다 룰이 다르다. 프리셋 + 편집 가능한 표.
5. **검증되지 않은 자동 수집은 열지 않는다.** 커스텀 매치 실측 전에는 자동 결과 수집을 보류하되,
   라우팅·계정·프로필·수동 결과 입력 등 선행 구조는 진행한다.

---

## 사이트 구조 — 게임 분리

롤과 배그는 **사이트 안에서 나뉜다.** 한 목록에 섞어 보여주지 않는다.

### URL 정책

| | 경로 | 대상 |
|---|---|---|
| 게임별 | `/lol/*` · `/pubg/*` | 내전, 내전 전적, 랭킹, 가이드, 프로필 |
| 공통 | 프리픽스 없음 | 클랜, 커뮤니티, 스트리머, 설정, 대시보드 |

URL만 봐도 게임 무관인지 드러나고, 같은 클랜 목록이 두 경로로 중복 색인되는 SEO 문제도 없다.
기존 경로(`/tournaments`, `/matches`, `/ranking`, `/guide`)는 `/lol/...` 로 301 리다이렉트한다.
이미 색인·공유된 링크가 있으므로 그냥 옮기면 안 된다.

헤더에 게임 스위처를 둔다. 링크에 게임이 담기므로 공유해도 받는 사람이 같은 화면을 본다.

### 무엇을 나누고 무엇을 공유하는가

**게임별로 나눈다**
- 내전(방 목록)·내전 전적·가이드 — 진행 방식 자체가 다르다
- **랭킹** — 롤은 티어·승률, 배그는 평균 순위·킬. 같은 표에 올릴 수 없다

**공유한다**
- 계정·친구·DM·평판·알림 — 사람은 한 명이다
- **클랜** — 한 클랜이 롤팀과 배그팀을 같이 굴리는 게 자연스럽다.
  게임별로 클랜을 따로 만들게 하면 같은 사람들이 클랜 둘을 관리해야 한다.
  클랜은 하나로 두고 **활동 기록·클랜 랭킹만 게임별**로 집계한다.
- **커뮤니티** — 게시판을 쪼개지 않는다. 실측 PV 550/일 규모에서 반으로 나누면
  양쪽 다 글이 안 올라오는 죽은 게시판이 된다. 게임 태그로 구분하고 목록은 합친다.
- **스트리머** — 사람 기준 목록. 방송하는 게임을 배지로 표시한다.

### 프로필

신원은 공통, 전적은 게임별이다.

```
/profile/:id       보는 사람의 현재 게임 컨텍스트로 리다이렉트
                   (그 사람이 해당 게임 미연동이면 연동된 게임으로 폴백)
/lol/profile/:id   상단 공통 + 롤 전적 (티어·라인·챔피언·내전 기록)
/pubg/profile/:id  상단 공통 + 배그 전적 (평균 순위·킬·스크림 이력)
```

상단(어느 게임에서 보든 동일): 닉네임·아바타·가입일·평판·클랜·연동된 게임 배지

**연동하지 않은 게임의 탭은 표시하지 않는다.** 배그를 안 하는 사람 프로필에
빈 배그 탭이 뜨면 안 된다. 설정(`/settings`)에서는 두 게임 연동을 나란히 둔다.

### 데이터 축

```prisma
enum GameTitle { LOL PUBG }

model Room {
  gameTitle GameTitle @default(LOL)   // 기존 방은 전부 LOL
  ...
}
```

게임별로 달라지는 값을 코드 한 곳에 모은다 (`packages/types/src/games/`):

| 항목 | LOL | PUBG |
|---|---|---|
| 팀 인원 | 5 | 모드별 설정 (킬내기 4인부터, 배틀로얄 최대 100명) |
| 팀 수 | `n / 5` | 모드·팀 크기·방장 설정에 따라 결정 |
| 포지션 | 5라인 | 없음 |
| 팀 편성 | 경매·스네이크·순환·자동밸런스·수동 | 경매·스네이크·순환·자동밸런스·수동 |
| 진행 | 역할선택 → 대진표 | 기존 2팀 경기 또는 라운드 반복 → 포인트 |
| 결과 수집 | 토너먼트 코드 | 커스텀 매치 자동 매칭 |

`riot` 모듈은 그대로 두고, 계정 연동·결과 수집·통계 세 지점만
게임 어댑터 인터페이스 뒤로 보낸다. 롤 어댑터는 기존 코드를 그대로 감싼다.

### 게임 모드와 팀 구성은 분리한다

`게임 모드`는 경기 규칙이고 `팀 구성 방식`은 참가자를 팀에 넣는 방법이다.
PUBG도 기존 팀 구성 방식을 모두 제공한다. 새로운 팀 구성 엔진을 별도로 만들지 않고,
팀 크기·팀 수·게임별 점수 계산만 설정으로 주입한다.

| PUBG 게임 모드 | 경기 구조 | 결과 |
|---|---|---|
| 킬내기 | 2팀, 4대4부터 확장 | 킬 수 또는 승패, 기존 Match/MatchSeries 재사용 가능 |
| 배틀로얄 내전 | 2~25팀, 최대 100명, 여러 라운드 | 순위 점수 + 킬 점수 누적 |
| 자유 매치 | 기존 수동 팀 배정 기반 | 결과 입력·전적 반영 여부를 선택 |

킬내기는 기존 2팀 승패·다전제·결과 보고 흐름을 재사용한다.
배틀로얄 내전만 다팀·다라운드 리더보드가 필요하므로 `Scrim` 계열 모델을 새로 만든다.
`자유 매치`는 별도 경기 엔진이 아니라 기존 `MANUAL_TEAM`을 이용한다.

### PUBG 팀 구성

경매·왕복형 스네이크·순환 픽·오토 밸런스·수동 팀 배정을 모두 유지한다.

- 왕복형 스네이크: `T1 → T2 → T3 → T4 → T4 → T3 → T2 → T1`
- 순환 픽: 기존 구현처럼 라운드 시작 팀을 순환
- 주장 선발: 랜덤·티어 우선·직접 지정·자원자
- 주장 선정과 픽 순서 추첨은 분리한다.
- 현재 스네이크 서비스의 5인 팀 하드코딩은 게임별 `teamSize`로 일반화한다.

PUBG의 `1티어·2티어·3티어`는 공식 PUBG 랭크와 다른 NEXUS 내부 편성 등급이다.
초기에는 운영자 지정 또는 승인 등급으로 시작하고, 이후 공식 랭크·평균 순위·평균 킬·
NEXUS 내전 성적을 이용해 자동 산정한다. PUBG에는 롤처럼 포지션별 점수 대신 전체
`편성 점수`를 둔다.

### PUBG 플랫폼 구분 — Steam / Kakao

PUBG 방 생성 시 플랫폼을 필수로 선택한다. 계정 등록에서 샤드를 묻는 것과 별개로,
이번 경기를 어느 플랫폼에서 진행할지는 방 설정으로 확정해야 한다.

```prisma
enum PubgPlatform {
  STEAM
  KAKAO
}
```

- 사용자는 `스팀 배그(스배)` 또는 `카카오 배그(카배)` 중 하나를 선택한다.
- 방 이름 원본은 보존하고 표시 제목에는 `[스배]` 또는 `[카배]`를 서버가 붙인다.
- 방 목록·로비·결과·Discord 공지·공유 미리보기에서 플랫폼 배지를 유지한다.
- PUBG 방은 플랫폼 없이는 생성할 수 없고, LoL 방에는 플랫폼 필드를 노출하지 않는다.
- 계정의 `lastMatchShard`와 방 플랫폼이 다르면 참가 전에 경고한다. 샤드 미확인 상태는
  허용하되, 불일치를 공식 인증 실패처럼 단정하지 않는다.

### 프로필·호버 프로필·계정 등록

사용자 신원과 게임 계정은 공유하지 않고, 현재 게임 라우트의 데이터만 보여준다.

```text
/lol/profile  → Riot 계정·LoL 랭크·라인·챔피언·LoL 내전 전적
/pubg/profile → PUBG 계정·공식 랭크·NEXUS 편성 등급·평균 순위·킬
```

- `/lol/...`에서 PUBG 정보를 표시하지 않는다.
- `/pubg/...`에서 Riot 티어·라인·챔피언을 표시하지 않는다.
- 호버 프로필과 로비 프로필 모달도 현재 `game` 컨텍스트를 그대로 전달한다.
- 공통 페이지(`/settings`, `/dashboard`)의 계정 추가는 게임 선택 후 해당 모달을 연다.
- LoL 등록은 기존 Riot ID·티어·라인·챔피언 흐름을 유지한다.
- PUBG 등록은 닉네임 조회 → Steam/Kakao 조회 → account ID 확인 → 등록 흐름으로 분리한다.
- PUBG API는 소유권을 직접 인증하지 않으므로 등록 화면에서 이를 명시한다.

공통 프로필에는 사용자명·아바타·클랜·평판·가입일·연결 게임만 둔다.
공식 PUBG 랭크와 NEXUS 편성 등급은 서로 다른 카드와 라벨로 표시한다.

## 배그 스크림 포맷

### 진행 흐름

```
방 생성 (배그, 모드·팀 크기·팀 수·스팀/카카오 선택)
  → 팀 편성 (경매 / 스네이크 / 순환 / 오토 밸런스 / 수동)
  → 라운드 1 시작
       호스트가 인게임에서 커스텀 매치 개설·진행
       Nexus 가 참가자 1명의 전적을 폴링 → isCustomMatch + 시작시각으로 해당 매치 식별
       매치 상세 1콜로 전 팀 순위·킬 수집 → 포인트 환산
  → 라운드 2, 3 … 반복
  → 누적 리더보드 확정
```

핵심은 **라운드 결과 자동 매칭**이다. 호스트가 매치 ID를 옮겨 적을 필요가 없어야 한다.
라운드 시작 시각 이후 + `isCustomMatch: true` + 참가자 명단 일치, 세 조건으로 특정한다.

### 새 모델

`Match`/`MatchSeries`는 2팀 고정이라 재사용하지 않는다.

단, 킬내기는 기존 2팀 승패·다전제 구조를 재사용할 수 있다. 다팀 배틀로얄 내전에서만
아래 모델을 사용한다.

```prisma
model Scrim            { roomId, totalRounds, pointRule, status }
model ScrimRound       { scrimId, roundNumber, pubgMatchId?, startedAt, status }
model ScrimTeamResult  { roundId, teamId, placement, kills, points }
```

방에는 PUBG 전용 경기 모드와 플랫폼도 함께 저장한다.

```prisma
enum PubgGameMode { KILL_MATCH BATTLE_ROYALE FREE_MATCH }
enum PubgPlatform { STEAM KAKAO }
```

`Room`의 `pubgPlatform`은 PUBG 방에서 필수이고, LoL 방에서는 null이다.

### 포인트 규칙

순위별 포인트 배열 + 킬당 포인트. 기본 프리셋 하나를 넣고 방마다 편집 가능하게 둔다.
(대회마다 표가 달라 "표준"을 코드에 못 박으면 매번 안 맞는다.)

---

## 재사용 / 신규 / 폐기

**그대로 재사용** — 계정·인증, 클랜, 커뮤니티, DM·친구·평판, 디스코드 봇(모집 공지·예약·음성채널),
방 로비·채팅, 관리자, 경매·스네이크·순환 픽·수동 팀 배정, 2팀 경기·다전제 결과 흐름

**새로 필요** — PUBG 계정 연동·랭크 스냅샷, PUBG 모드/플랫폼 설정, 다팀 스크림 진행(라운드·결과 수집·포인트),
배틀로얄 리더보드, PUBG 전적, PUBG 편성 점수

**배그에서 숨김** — 역할 선택, 챔피언 설정, LoL 라인 밸런스, LoL 전용 통계

---

## 작업 목록

### Phase 0 — 자동 수집 전 검증 (자동화 착수 전 필수)

실측 도구: `scripts/pubg-probe.sh <닉네임>` — 운영 컨테이너의 `PUBG_API_KEY`로
샤드 판별 → 최근 매치 훑기 → 팀별 순위·킬·명단 추출까지 한 번에 본다.

- [x] Task 1: PUBG 개발자 키 발급 + 샤드 구분 — 키는 GitHub Secrets 경유로 배포에 주입.
      **닉네임 조회는 샤드와 무관하다.** 계정 여러 개를 steam·kakao 양쪽에
      물었더니 **동일한 `account.xxx` ID**가 돌아왔다.
      응답의 `shardId`는 요청한 샤드를 그대로 되돌려줄 뿐 계정의 소속이 아니다.
      **갈리는 것은 매치 기록이다** — 스팀으로 플레이하는 계정은 steam 20건 / kakao 0건.

      반대 방향도 확인했다 — 카카오로 플레이하는 계정 하나는 steam 0건 / **kakao 86건**으로,
      카카오 플레이 계정은 kakao 샤드에서만 매치가 나온다.
      kakao 샤드의 매치 응답 구조는 steam과 동일했다(팀 16개, 순위·킬·명단 정상).

      → 계정 연동에서 사용자에게 샤드를 묻지 않는다. 닉네임으로 계정 ID를 확정하고,
        매치는 두 샤드를 훑어 나오는 쪽을 `PubgAccount`에 기억해 이후 예산을 아낀다.
      → 주의: 로스터 인원이 항상 팀 정원과 같지는 않다(3인 로스터 관측).
        포인트 집계는 로스터 인원수를 가정하지 말고 실제 참가자로 계산한다.
- [x] Task 2: 매치 응답 구조 실측 (2026-09-03)
      `data.attributes` — `createdAt`, `gameMode`(squad), `mapName`, **`isCustomMatch`**
      `included[type=roster]` — 팀별 `attributes.stats.rank`(순위)
      `included[type=participant]` — `attributes.stats`의 `name`, `kills`
      매치 1건에 **전 팀·전원**이 들어온다. 16팀 매치에서 팀별 순위·합산 킬·명단을
      전부 뽑아냈다.
- [ ] Task 3: 자동 매칭 — **보류. 구조는 검증됐고 커스텀 판별만 남았다**
      실제 커스텀 매치가 있어야만 확인되는 항목이라, 여기 묶여 있지 않고 다음 단계로 간다.
      Phase 5(배틀로얄 내전 자동 수집) 착수 전에는 반드시 닫아야 한다.
      일반 매치로 "순위·킬·참가자 명단 전부 확보"까지 확인했다. 남은 것은
      `isCustomMatch: true` 인 매치가 같은 구조로 오는지와, `createdAt` + 참가자 명단으로
      해당 라운드를 특정할 수 있는지다. **커스텀 매치를 한 판 치른 뒤 재실행하면 끝난다.**
- [x] Task 4: 레이트 예산 — **10req/분으로 충분하다.**
      라운드당 `플레이어 전적 1 + 매치 상세 1` = 2콜. 16팀 64명 매치도 매치 1건에
      전원이 들어오므로 인원수에 비례하지 않는다. 상향 신청 불필요.
- [ ] Task 5: 커스텀 매치 개설 권한 — **보류. 미확인이며 이 기획 최대의 위험**
      인게임에서 커스텀 방을 한 번 만들어보면 즉시 판명된다. Task 3과 같이 해결된다.
      Phase 5 착수 전에는 반드시 닫아야 한다 — 막혀 있으면 자동 수집형 스크림이 성립하지 않는다.
      과거에는 파트너 스트리머 전용이었고 이후 전체 개방됐다는 기록이 있으나
      2026년 현재 정책은 확인하지 못했다. 인게임에서 커스텀 매치 생성이 되는지
      직접 확인하는 것이 가장 빠르다. 제한이 있다면 롤 토너먼트 코드와 같은 병목이 된다.

### Phase 1 — 게임 축 + 라우팅 분리 (배그 기능 없이, 롤 동작 그대로)

- [x] Task 6: `GameTitle` enum + `Room.gameTitle` 추가 (기본값 LOL, 마이그레이션)
- [x] Task 7: 게임별 설정 테이블 신설 (`packages/types/src/games/`) — 팀 인원·팀 수·포지션 유무·가능한 팀 편성 모드
- [~] Task 8: 팀 수·팀 크기 계산을 게임/모드 설정으로 일반화 + 롤 호출부 회귀 확인
- [~] Task 9: 라우트를 `/lol/*` 아래로 이동 (내전·전적·랭킹·가이드·프로필)
- [x] Task 10: 기존 경로 → `/lol/*` 리다이렉트 + 사이트맵 갱신 (2026-09-04)
      **배포 차단 요소였다.** 라우트가 `[game]` 아래로 옮겨졌는데 리다이렉트가 없어서,
      현재 main 을 배포하면 `/matches`·`/ranking`·`/guide/*`·`/tournaments` 등
      기존 링크가 전부 404 가 되는 상태였다(운영은 아직 `2dd3ba3a` 라 안 깨져 있었다).
  - `next.config.mjs` 에 8개 경로 × (자기 자신 + `:rest*`) 영구 리다이렉트
  - 사이트맵을 최종 URL(`/lol/*`)로 교체 — 리다이렉트되는 URL 을 올리면 크롤 예산이 샌다
  - 디스코드 봇이 만들던 `/tournaments/:id/lobby` 링크 7곳을 게임별 URL 헬퍼로 교체
    (`common/utils/app-url.util.ts`). 모집 공지마다 나가는 링크라 한 번 보내면 못 고친다.
  - **부수 발견**: `[game]/layout.tsx` 의 `notFound()` 가 동작하지 않아
    `/foobar/matches` 가 롤 페이지를 200 으로 렌더하고 `/zzz-not-real` 도 200 이었다.
    layout 과 page 가 병렬 렌더돼서 그렇다. `generateStaticParams` +
    `dynamicParams = false` 로 세그먼트 단계에서 막았다. 아무 URL 이나 페이지가 되면
    검색엔진이 무한 중복 문서를 색인한다.
  - 검증(로컬 프로덕션 빌드): 옛 경로 8종 308 리다이렉트, 목적지 200,
    공통 경로(클랜·커뮤니티·스트리머·설정) 프리픽스 없이 200,
    잘못된 슬러그 4종 404
- [x] Task 11: 헤더 게임 스위처 + 현재 게임 컨텍스트 (2026-09-04)
  - `useCurrentGame()` — 경로 첫 칸에서 게임을 읽고, 게임 무관 화면에서는 기본 게임으로 폴백
  - `GameSwitcher` — 헤더(sm 이상)와 모바일 메뉴에 노출. 게임을 바꾸면 그 게임의
    내전 목록으로 보낸다. 현재 경로를 그대로 갈아끼우면 `/lol/matches/match/xxx` 같은
    롤 전용 상세가 배그에서 깨진다.
  - 헤더·모바일 메뉴 네비를 게임별/공통으로 분리. 옛 경로를 그대로 쓰면 매 클릭마다
    리다이렉트를 한 번 더 타고, pathname 과 href 가 달라 활성 표시도 깨진다.
  - `GameDefinition.shortLabel` 추가(자리 좁은 스위처용)
  - 배그는 `enabled: true` 라 스위처에 노출된다. 준비 중 처리는 `[game]/layout.tsx` 가
    `enabled:false` 일 때 안내 화면을 내보내는 쪽으로 이미 되어 있다.
  - 검증: `/lol/*` 에서 헤더 링크가 전부 `/lol/*`, `/pubg/*` 에서 전부 `/pubg/*`
  - 남은 것: `Footer` 는 서버 컴포넌트라 게임을 못 읽어 가이드 링크가 `/lol/guide` 고정이다
- [~] Task 12: 공통 영역(클랜·커뮤니티·스트리머·설정)은 프리픽스 없이 유지되는지 확인 + 내부 링크 정리
- [ ] Task 13: `/profile/:id` 리다이렉트 규칙 — 현재 게임 컨텍스트 우선, 미연동이면 폴백
- [ ] Task 14: 게임별 API·프로필·계정·결과 수집 경계 정의 (롤 기존 코드 회귀 없음)

### Phase 2 — PUBG 계정 연동

- [ ] Task 15: `PubgAccount` 모델 — 닉네임·accountId·`lastMatchShard` (사용자에게 샤드를 먼저 묻지 않음)
      샤드는 사용자에게 묻지 않는다(Task 1 실측). 매치가 나온 샤드를 기억해
      다음 조회부터 그쪽만 본다.
- [ ] Task 16: 닉네임 → accountId 조회 + Steam/Kakao 탐색 + 캐시 (전역 레이트 캡에 편입)
- [ ] Task 17: 소유권 검증 한계 명시 + 중복 accountId 등록 차단
- [ ] Task 18: `/pubg` 프로필·호버 프로필·로비 모달에 PUBG 데이터 표시
- [ ] Task 19: 게임별 계정 등록 모달 분리 — LoL 기존 3단계 유지, PUBG 닉네임 조회 흐름 추가
- [ ] Task 20: 공통 설정에서 게임 선택 후 해당 계정 등록 모달 열기
- [ ] Task 21: PUBG 공식 랭크 스냅샷 저장 — API에서 확인된 시즌·모드·티어만 표시
- [ ] Task 22: NEXUS 편성 등급·전체 편성 점수 모델 — 운영자 지정/자동 산정/데이터 부족 상태 구분

### Phase 3 — PUBG 방·팀 구성

- [ ] Task 23: PUBG 방 생성에 경기 모드·팀 크기·팀 수·Steam/Kakao 플랫폼 필수 설정 추가
- [ ] Task 24: 방 제목 `[스배]`/`[카배]` 접두사와 플랫폼 배지 공통 포맷터 추가
- [ ] Task 25: 방 목록·로비·Discord 공지·공유 미리보기 플랫폼 표시 및 스배/카배 필터 추가
- [ ] Task 26: 기존 경매·왕복형 스네이크·순환 픽·수동 팀 배정을 PUBG 팀 크기에 맞게 일반화
- [ ] Task 27: 주장 선발과 픽 순서 추첨 분리 — 랜덤·티어 우선·직접 지정·자원자
- [ ] Task 28: PUBG 오토 밸런스 1차 — 운영자 지정 NEXUS 티어 기반, 롤 라인 점수와 분리
- [ ] Task 29: 100명 참가자 기준 로비 소켓·프로필 지연 조회·권한·동시 픽 검증

### Phase 4 — 킬내기

- [ ] Task 30: 킬내기 2팀 흐름 — 기존 Match/MatchSeries·승패 보고 재사용
- [ ] Task 31: 4대4부터 가변 팀 크기·다전제 프리셋 지원
- [ ] Task 32: 팀별 총 킬·개인 킬 결과 입력 및 확정
- [ ] Task 33: PUBG 킬내기 전적·프로필 요약·Discord 결과 공지

### Phase 5 — 배틀로얄 내전

- [ ] Task 34: `Scrim`·`ScrimRound`·`ScrimTeamResult` 모델 + 마이그레이션
- [ ] Task 35: 스크림 생성 — 라운드 수·순위 점수표·킬 점수 설정
- [ ] Task 36: 라운드 시작/종료 상태 관리 + 소켓 게이트웨이
- [ ] Task 37: 자동 매칭 — 참가자 전적 폴링 → 커스텀 매치 식별 → 상세 수집
- [ ] Task 38: 자동 매칭 실패 시 호스트가 매치 ID를 직접 넣는 수동 경로
- [ ] Task 39: 포인트 환산·누적 집계·라운드별/전체 리더보드


### 로비 개선 (게임 공통 + 배그 대비)

로비가 **게임 설정을 전혀 참조하지 않는다** (2026-09-04 실측):
`gameTitle`·`hasPositions`·`GAMES[...]` 참조 0건인데, 라인/포지션 관련 코드가 146곳,
티어가 36곳, 챔피언이 18곳이다. 배그 방을 열면 없는 개념의 UI가 그대로 나온다.
`page.tsx` 1,382줄 + `AutoBalanceReview` 1,025줄로 덩치도 크다.

- [ ] Task 46: 로비를 게임 설정 기반으로 분기 — `hasPositions=false`면 라인 선택·라인 배지·
      역할 선택 진입을 숨긴다. 조건 분기를 화면 곳곳에 뿌리지 말고 게임별 설정 한 곳에서 읽는다
- [ ] Task 47: 참가자 카드의 티어·챔피언 표시를 게임별로 교체 — 롤은 솔로랭크 티어,
      배그는 공식 랭크 스냅샷 또는 NEXUS 편성 등급(Task 21·22와 연결)
- [ ] Task 48: 대규모 인원 렌더 — 현재 참가자를 전부 `.map()` 으로 편다. 롤 10~40명은
      버티지만 배그는 64~100명이다. 팀 접기/펼치기 또는 가상화, 프로필 조회 지연 로딩
- [ ] Task 49: 팀 구획 표시를 `teamSize` 기반으로 일반화 — 4인 스쿼드 16팀 같은 구성에서
      2열 그리드가 무너진다. 팀 수가 많을 때의 레이아웃을 따로 정한다
- [ ] Task 50: `page.tsx`(1,382줄) 분할 — 상태·소켓 배선과 화면을 나눠 게임별 분기를
      넣을 수 있는 크기로 만든다. Task 46~49의 선행 작업

### Phase 6 — 결과 노출

- [ ] Task 40: 배틀로얄 리더보드 화면 (라운드별 + 누적)
- [ ] Task 41: PUBG 전적 — 스크림·킬내기 참가 이력·평균 순위·킬
- [ ] Task 42: Discord 모집 공지·결과 공지를 PUBG 포맷으로 (기존 봇 파이프라인 재사용)

### Phase 7 — 편성 점수 고도화

- [ ] Task 43: PUBG 공식 랭크·평균 순위·평균 킬·NEXUS 내전 성적 기반 편성 점수 산식 확정
- [ ] Task 44: `1티어·2티어·3티어` 자동 산정·운영자 보정·등급 이력
- [ ] Task 45: 오토 밸런스 점수 버전·샘플 수·산정 시각 표시 및 재계산

---

## 측정

배그를 붙인 뒤 4주간 본다. 이 숫자가 안 움직이면 방향을 바꾼다.

| 지표 | 현재 | 목표 |
|---|---|---|
| 배그 스크림 개설 | 0 | 주 1+ |
| 정원 충족 스크림 | 0 | 1+ |
| 라운드 결과 자동 매칭 성공률 | - | 90%+ |
| 배그 계정 연동 수 | 0 | 20+ |

---

## 리스크 / 미결

- **콜드스타트가 두 배가 된다.** 롤 내전조차 아직 정원 충족 0회다
  ([TODO_discord_bot_activation.md](./TODO_discord_bot_activation.md) 측정표).
  배그 쪽 커뮤니티를 실제로 데려올 수 있는지가 기술보다 앞선 조건이다.
- **계정 소유권 검증이 약하다.** 롤은 Riot 인증이 있지만 배그는 없다. Task 17이 이 기획에서
  가장 불확실한 항목이고, 사칭을 완전히 막을 방법은 없을 수 있다.
- **2주 보존.** 라운드가 끝나고 방치되면 결과를 영영 못 받는다. 수동 입력 경로(Task 38)가 보험이다.
- **커스텀 매치 개설 권한**(Task 5)이 제한적이라면 롤 토너먼트 코드와 같은 병목이 된다.
  Phase 5 자동 수집 전에 확인해야 한다.
- 샤드 문제는 해소됐다(Task 1). steam·kakao 양쪽 모두 실측으로 확인했다.
- 엔드포인트 경로·응답 스키마는 2026-09-03 실측으로 확인됐다 (Task 2).

## 참고

- [PUBG Developer FAQ](https://developer.pubg.com/faq) — 커스텀 매치 2주 보존, `isCustomMatch`, API로 생성 불가
- [Rate Limits](https://documentation.pubg.com/en/rate-limits.html) — 기본 10req/분, 상향 신청
- 롤 쪽 같은 문제의 기록: [TODO_riot_account_identity_revamp.md](./TODO_riot_account_identity_revamp.md)
