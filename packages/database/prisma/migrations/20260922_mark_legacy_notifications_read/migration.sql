-- 헤더 알림(종 아이콘)을 처음 여는 배포다.
--
-- 알림은 그동안 저장만 되고 화면에 나온 적이 없다(종 아이콘을 담은 NavBar 가
-- 초기에 Header 로 교체되며 빠졌다). 그대로 열면 몇 달 치 알림이 전부 "안 읽음"
-- 으로 한꺼번에 뜬다. 기록은 남기고 읽음으로만 돌려, 배지가 0 에서 시작하게 한다.
--
-- 데이터만 바꾸는 마이그레이션이라 스키마 변화는 없다. 두 번 돌아도 결과가 같다.
UPDATE "notifications" SET "isRead" = true WHERE "isRead" = false;
